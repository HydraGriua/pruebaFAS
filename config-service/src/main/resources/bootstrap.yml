server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/HydraGriua/pruebaFAS.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          default-label: "dev"
  security:
    user:
      name: pruebateodio
      password: pruebateodio123
